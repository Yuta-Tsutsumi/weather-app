import React from "react";
import styles from "./GoogleMap.module.scss";

const GoogleMap: React.VFC = () => {
  return <div className={styles.root}>google mapをここに実装する。</div>;
};

export default GoogleMap;
